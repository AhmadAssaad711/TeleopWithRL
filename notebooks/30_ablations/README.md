# Ablations

`30_reward_state_ablation.ipynb` compares controlled reward and observation
variants. The training implementation is in `matlab_env_python_replica/dqn/`;
the notebook owns configuration, comparison tables, and visual review.
